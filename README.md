# RedDeviz

Dataviz avec tous les matchs des Diables Rouges.

```sh
npm install
npm run dev      # développement
npm run build    # build statique → build/
```

Déployé sur GitHub Pages à chaque push sur `main`.
